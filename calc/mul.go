package calc

// mul computes the product of a and b.
//
// TODO(implement): replace this stub with the real multiplication so that the
// RED unit tests (calc_test.go) and the RED e2e test (e2e/e2e_test.go) pass.
// The stub exists only so the package compiles while the tests are RED; it is
// intentionally NOT part of the protected interface, so this is the file to
// edit during implementation.
func mul(a, b int) int {
	panic("calc: Mul not implemented")
}

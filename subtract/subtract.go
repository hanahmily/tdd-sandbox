// Package subtract provides integer subtraction.
package subtract

// Difference returns the result of subtracting b from a (a - b).
//
// NOTE: this is an unimplemented stub. It exists only so the package
// compiles; the RED tests in this package fail until a real
// implementation replaces the panic below.
func Difference(a, b int) int {
	panic("subtract.Difference: not implemented")
}

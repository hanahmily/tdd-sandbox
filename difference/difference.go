// Package difference provides integer subtraction.
package difference

// Sub returns the difference of a and b, i.e. a - b.
//
// This is a RED stub: it compiles so the package builds and the test suite
// runs, but it does not yet implement the contract, so every test fails until
// a real implementation replaces this body.
func Sub(a, b int) int {
	panic("difference.Sub not implemented")
}

// Package multiply provides integer multiplication helpers.
package multiply

// Product returns the product of two integers.
//
// The contract is defined by the RED suite in this package; the
// implementation is intentionally a stub until the coder makes the tests
// pass.
func Product(a, b int) int {
	// Not implemented: returns the zero value so the package compiles while
	// the RED tests fail.
	return 0
}

// Package difference provides integer subtraction.
package difference

// Sub returns the difference of a and b, i.e. a - b.
//
// This is a RED stub: it panics so the accompanying tests fail until a real
// implementation is provided. It must not return a value, because a zero
// return would spuriously satisfy the zero-operand case (Sub(0, 0) == 0).
func Sub(a, b int) int {
	panic("difference: Sub not implemented")
}
